mission main {
  probe log(level: Str, message: Str) -> Void;

  let mut count: Int = 0;

  telemetry
    while count lt 3 do
      call log(level: "info", message: int_to_str(count));
      set count = count plus 1;
    end
  end

  print("mission complete");
}